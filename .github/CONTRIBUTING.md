# Contributing to MOMOKIKI

1. Fork → branch `feature/<name>` or `fix/<name>`
2. Run `flutter analyze` — must pass with 0 warnings
3. Run `flutter test` — must pass
4. PR → main (squash merge)

## Code style

See `plans/AGENT-RULES.md`
